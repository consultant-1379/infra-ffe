#Only Listen on the public IP address and the services IP address
ListenAddress ${public_ip}
ListenAddress 192.168.0.1
#Only Listen on the Ipv4 address
AddressFamily inet
# Enable password authentication
PasswordAuthentication yes